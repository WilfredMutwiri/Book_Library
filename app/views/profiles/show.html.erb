<h1>Your Profile</h1>

<h2>Borrowed Books</h2>
<ul>
  <% @user.borrowed_books.each do |book| %>
    <li>
      <%= book.title %> by <%= book.author %> (Due: <%= book.borrowings.last.due_date %>)
      <%= button_to 'Return', return_book_path(book), method: :post %>
    </li>
  <% end %>
</ul>
