borrowing_one:
  user: users(:one)
  book: books(:one)
  borrowed_at: <%= Time.now %>
  due_date: <%= 2.weeks.from_now %>

borrowing_two:
  user: users(:two)
  book: books(:two)
  borrowed_at: <%= Time.now %>
  due_date: <%= 2.weeks.from_now %>
